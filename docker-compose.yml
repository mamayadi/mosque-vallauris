services:
  web:
    build: .
    ports:
      - "4321:4321"
    volumes:
      # Hot-reload: mount source and node_modules separately
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    command: npm run dev
